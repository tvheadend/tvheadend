name: Build Tvheadend CI

on:
  pull_request:
    branches:
      - master
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

concurrency:
  # auto-cancel previous runs if the pull request is force-push updated
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  vue:
    # Build the Vue UI once; every package job below depends on this and
    # consumes the dist via a GitHub artifact (no per-build pcloud fetch).
    # No secrets are passed — PR CI is pcloud-token-free (matching the
    # matrix jobs); the gate still builds and publishes the artifact, and
    # the pcloud upload runs only from build-cloudsmith on master.
    uses: ./.github/workflows/webui-vue-build.yml

  build-raspberry:
    needs: vue
    runs-on: ubuntu-latest
    continue-on-error: true
    name: Build on Raspberry Pi ${{ matrix.arch }}

    strategy:
      matrix:
        arch: [armv6l, armv7l, aarch64]
        include:
        - arch: armv6l
          cpu: arm1176
          base_image: raspios_lite:latest
          cpu_info: cpuinfo/raspberrypi_zero_w
        - arch: armv7l
          cpu: cortex-a7
          base_image: raspios_lite:latest
          cpu_info: cpuinfo/raspberrypi_3b
        - arch: aarch64
          cpu: cortex-a53
          base_image: raspios_lite_arm64:latest
          cpu_info: cpuinfo/raspberrypi_zero2_w_arm64
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Download Vue UI artifact
      uses: actions/download-artifact@v4
      with:
        name: ${{ needs.vue.outputs.artifact_name }}
        path: .vuecache
    - uses: pguyot/arm-runner-action@v2
      with:
        base_image: ${{ matrix.base_image }}
        cpu: ${{ matrix.cpu }}
        cpu_info: ${{ matrix.cpu_info }}
        image_additional_mb: 10000
        copy_artifact_path: ../tvheadend*.deb
        import_github_env: true
        commands: |
          sudo apt-get update -y
          sudo apt-get install --force-yes -y cmake git build-essential pkg-config gettext libavahi-client-dev libssl-dev zlib1g-dev wget bzip2 git-core liburiparser-dev libdvbcsa-dev python3 python3-requests debhelper ccache lsb-release libc6-dev-armel-cross
          DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre3-dev || DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre2-dev
          VUE_FILE_CACHE="$PWD/.vuecache" AUTOBUILD_CONFIGURE_EXTRA="--enable-ccache --enable-ffmpeg_static --enable-hdhomerun_static --python=python3 --enable-vue_cache --disable-vue_build" ./Autobuild.sh -p raspios
          support/cloudsmith.sh -n -p raspios -f '../tvheadend*.deb'
    - uses: actions/upload-artifact@v4
      with:
        name: Tvheadend-RPiOS-${{ matrix.arch }}
        path: tvheadend*.deb
        if-no-files-found: error

  build-crosscompile:
    # The host should always be linux
    runs-on: ubuntu-latest
    needs: vue
    continue-on-error: true
    name: Build on ${{ matrix.distro }} ${{ matrix.arch }}
    # Run steps on a matrix
    strategy:
      matrix:
       arch: [ armv7, aarch64]
       distro: [ stretch, buster, bullseye, bookworm, ubuntu16.04, ubuntu18.04, ubuntu20.04, ubuntu22.04, ubuntu24.04 ]
       include:
         - arch: armv6
           distro: stretch
         - arch: armv6
           distro: buster
         - arch: armv6
           distro: bullseye
         - arch: aarch64
           distro: ubuntu_latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Download Vue UI artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.vue.outputs.artifact_name }}
          path: artifacts/.vuecache
      - uses: uraimo/run-on-arch-action@v3
        name: Build artifact
        id: build
        with:
          arch: ${{ matrix.arch }}
          distro: ${{ matrix.distro }}

          # Not required, but speeds up builds
          githubToken: ${{ github.token }}

          # Create an artifacts directory
          setup: |
            mkdir -p "${PWD}/artifacts"

          # Mount the artifacts directory as /artifacts in the container.
          # The Vue dist tarball is downloaded into artifacts/.vuecache so
          # it rides in on this same mount — a second --volume would make
          # dockerRunArgs multi-line, which run-on-arch mis-parses.
          dockerRunArgs: |
            --volume "${PWD}/artifacts:/artifacts"

          # Pass environment variables to the container.
          env: | # YAML, but pipe character is necessary
            artifact_name: git-${{ matrix.distro }}_${{ matrix.arch }}


          # The shell to run commands with in the container
          shell: /bin/sh

          # Install some dependencies in the container. This speeds up builds if
          # you are also using githubToken. Any dependencies installed here will
          # be part of the container image that gets cached, so subsequent
          # builds don't have to re-install them. The image layer is cached
          # publicly in your project's package repository, so it is vital that
          # no secrets are present in the container state or logs.
          install: |
            if [ "${{ matrix.distro }}" = "buster" ] && [ "${{ matrix.arch }}" = "armv6" ]; then
              echo "Skipping source list modification for buster on armv6"
            else
              case "${{ matrix.distro }}" in
                jessie|stretch|buster)
                  sed -i 's/archive.raspbian.org/legacy.raspbian.org/g' /etc/apt/sources.list
                  sed -i 's/deb.debian.org/archive.debian.org/g' /etc/apt/sources.list
                  sed -i 's/security.debian.org/archive.debian.org/g' /etc/apt/sources.list
                  sed -i '/${{ matrix.distro }}-updates/d' /etc/apt/sources.list
                  ;;
              esac
            fi
            case "${{ matrix.distro }}" in
              ubuntu*|jessie|stretch|buster|bullseye|bookworm|trixie)
                apt-get update -y
                DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y cmake git build-essential pkg-config gettext libavahi-client-dev libssl-dev zlib1g-dev wget bzip2 git-core liburiparser-dev libdvbcsa-dev python3 python3-requests debhelper ccache lsb-release libc6-dev-armel-cross
                DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre3-dev || DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre2-dev
                ;;
              fedora*)
                dnf -y update
                dnf -y install git which
                ;;
              alpine*)
                apk update
                apk add git
                ;;
            esac

          # Produce a binary artifact
          run: |
            git config --global --add safe.directory /home/runner/work/tvheadend/tvheadend || true
            export VUE_FILE_CACHE=/artifacts/.vuecache
            AUTOBUILD_CONFIGURE_EXTRA="--enable-ccache --enable-ffmpeg_static --enable-hdhomerun_static --python=python3 --enable-vue_cache --disable-vue_build" ./Autobuild.sh
            cp ../tvheadend*.deb /artifacts/
            if [ '${{ matrix.distro }}' = 'buster' ] && [ '${{ matrix.arch }}' = 'armv7' ]; then update-ca-certificates --fresh; fi
            support/cloudsmith.sh -n -f '../tvheadend*.deb'
      - uses: actions/upload-artifact@v4
        with:
          name: Tvheadend-${{ matrix.distro }}-${{ matrix.arch }}
          path: artifacts/tvheadend*.deb
          if-no-files-found: error

  build-deb-native:
    runs-on: ubuntu-latest
    needs: vue
    continue-on-error: true
    name: Build on native ${{ matrix.container[1] }}

    strategy:
      matrix:
        container: [["i386/ubuntu:trusty", "i386-ubuntu-trusty"], ["ubuntu:trusty", "ubuntu-trusty"], ["i386/ubuntu:xenial", "i386-ubuntu-xenial"], ["ubuntu:xenial", "ubuntu-xenial"], ["ubuntu:bionic", "ubuntu-bionic"], ["ubuntu:focal", "ubuntu-focal"], ["ubuntu:jammy", "ubuntu-jammy"], ["ubuntu:noble", "ubuntu-noble"], ["ubuntu:plucky", "ubuntu-plucky"], ["ubuntu:questing", "ubuntu-questing"], ["ubuntu:resolute", "ubuntu-resolute"], ["i386/debian:stretch", "i386-debian-stretch"], ["debian:stretch", "debian-stretch"], ["i386/debian:buster", "i386-debian-buster"], ["debian:buster", "debian-buster"], ["i386/debian:bullseye", "i386-debian-bullseye"], ["debian:bullseye", "debian-bullseye"], ["i386/debian:bookworm", "i386-debian-bookworm"], ["debian:bookworm", "debian-bookworm"], ["i386/debian:trixie", "i386-debian-trixie"], ["debian:trixie", "debian-trixie"], ["i386/debian:sid", "i386-debian-sid"], ["debian:sid", "debian-sid"]]
    steps:
      - name: Setup container
        run: |
          docker pull ${{ matrix.container[0] }}
          docker run --name build-container -d -e GITHUB_ACTIONS=true -v ${{ github.workspace }}:/workspace ${{ matrix.container[0] }} tail -f /dev/null
      - name: Fix old debian apt
        if: matrix.container[0] == 'debian:stretch' || matrix.container[0] == 'i386/debian:stretch' || matrix.container[0] == 'debian:buster' || matrix.container[0] == 'i386/debian:buster'
        env:
          SCRIPT: |
            sed -i 's/deb.debian.org/archive.debian.org/g' /etc/apt/sources.list
            sed -i 's/security.debian.org/archive.debian.org/g' /etc/apt/sources.list
            sed -i '/stretch-updates/d' /etc/apt/sources.list
        run: docker exec build-container bash -c "$SCRIPT"
      - name: dependencies
        env:
          SCRIPT: |
            apt-get update -y
            DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y cmake git build-essential pkg-config gettext libavahi-client-dev libssl-dev zlib1g-dev wget bzip2 git-core liburiparser-dev libdvbcsa-dev python3 python3-requests debhelper ccache lsb-release
        run: docker exec build-container bash -c "$SCRIPT"
      - name: pcre-dependency
        env:
          SCRIPT: |
            DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre3-dev || DEBIAN_FRONTEND=noninteractive apt-get install --force-yes -y libpcre2-dev
        run: docker exec build-container bash -c "$SCRIPT"
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Download Vue UI artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.vue.outputs.artifact_name }}
          path: ${{ github.workspace }}/.vuecache
      - name: Workaround safe directory
        env:
          SCRIPT: |
            git config --global --add safe.directory /workspace
        run: docker exec build-container bash -c "$SCRIPT"
      - name: Cache static dependencies
        uses: actions/cache@v4
        with:
          path: .staticlib
          key: staticlib-${{ matrix.container[1] }}-${{ github.sha }}
          restore-keys: |
            staticlib-${{ matrix.container[1] }}-
      - name: build
        env:
          SCRIPT: |
            cd /workspace
            mkdir -p .staticlib
            export STATICLIB_CACHE=/workspace/.staticlib
            export VUE_FILE_CACHE=/workspace/.vuecache
            AUTOBUILD_CONFIGURE_EXTRA="--enable-ccache --enable-ffmpeg_static --enable-hdhomerun_static --python=python3 --enable-vue_cache --disable-vue_build" ./Autobuild.sh ${{ (startsWith(matrix.container[0], 'i386') && '-a i386') || '' }}
        run: docker exec build-container bash -c "$SCRIPT"
      - name: copy-result
        env:
          SCRIPT: |
            cp tvheadend*.deb workspace/
        run: docker exec build-container bash -c "$SCRIPT"
      - uses: actions/upload-artifact@v4
        with:
          name: Tvheadend-${{ matrix.container[1] }}
          path: tvheadend*.deb
          if-no-files-found: error
      - name: upload-cloudsmith
        env:
          SCRIPT: |
            cd /workspace
            support/cloudsmith.sh -n -f 'tvheadend*.deb'
        run: docker exec build-container bash -c "$SCRIPT"

  build-rpm-native:
    needs: vue
    runs-on: ubuntu-latest
    continue-on-error: true
    name: Build on native fedora:${{ matrix.releasever }}

    strategy:
      matrix:
        releasever: ["37", "38", "39", "40", "41", "42", "43", "44", "rawhide"]
    container:
      image: "fedora:${{ matrix.releasever }}"
    steps:
      - name: rpmfusion-free
        run: |
          dnf install -y "https://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-${{ matrix.releasever }}.noarch.rpm"
      - name: dependencies
        run: |
          dnf install -y gcc-c++ gcc-c++ which rpm-build rpmdevtools git make cmake gettext-devel dbus-devel avahi-devel openssl-devel zlib-devel libdvbcsa-devel wget bzip2 uriparser-devel pcre2-devel python python-requests ccache systemd-units systemd-devel
          dnf install -y openssl-devel-engine || true
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          tags: true
      - name: Download Vue UI artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.vue.outputs.artifact_name }}
          path: .vuecache
      - name: Workaround safe directory
        run: git config --global --add safe.directory "$(pwd)"
      - name: Cache static dependencies
        uses: actions/cache@v4
        with:
          path: .staticlib
          key: staticlib-fedora-${{ matrix.releasever }}-${{ github.sha }}
          restore-keys: |
            staticlib-fedora-${{ matrix.releasever }}-
      - name: build
        run: |
          mkdir -p .staticlib
          export STATICLIB_CACHE="$PWD/.staticlib"
          export VUE_FILE_CACHE="$PWD/.vuecache"
          export TVH_EXTRA_CONFIGURE="--enable-vue_cache --disable-vue_build"
          ./configure --disable-dvbscan --disable-libfdkaac_static --disable-ffmpeg_static --disable-hdhomerun_static --disable-libfdkaac_static --disable-libopus_static --disable-libtheora_static --disable-libvorbis_static --disable-libvpx_static --disable-libx264_static --disable-libx265_static --enable-libfdkaac --enable-hdhomerun_client --enable-libsystemd_daemon --python=/usr/bin/python3 $TVH_EXTRA_CONFIGURE && make -C rpm build -j$(nproc) || (echo "PARALLEL BUILD FAILED, DOING SINGLE THREADED BUILD" && make -C rpm build)
      - name: copy-result
        run: cp rpm/RPMS/*/tvheadend*.rpm .
      - uses: actions/upload-artifact@v4
        with:
          name: Tvheadend-Fedora-${{ matrix.releasever }}
          path: tvheadend*.rpm
          if-no-files-found: error
      - name: upload-cloudsmith
        run: support/cloudsmith.sh -n -f 'tvheadend*.rpm'

  build-el-rpm-native:
    needs: vue
    continue-on-error: true
    name: Build on native almalinux:${{ matrix.releasever }} ${{ matrix.runner == 'ubuntu-latest' && 'x86_64' || 'aarch64' }}
    strategy:
      matrix:
        releasever: ["9", "10"]
        runner: ["ubuntu-latest", "ubuntu-24.04-arm"]
    runs-on: ${{ matrix.runner }}

    container:
      image: "almalinux:${{ matrix.releasever }}"
    steps:
      - name: rpmfusion-free
        run: |
          dnf install -y dnf-plugins-core
          dnf config-manager --set-enabled crb
          dnf install -y "https://download1.rpmfusion.org/free/el/rpmfusion-free-release-${{ matrix.releasever }}.noarch.rpm"
      - name: dependencies
        run: |
          dnf install -y gcc-c++ gcc-c++ which rpm-build rpmdevtools git make cmake gettext-devel dbus-devel avahi-devel openssl-devel zlib-devel libdvbcsa-devel wget bzip2 uriparser-devel pcre2-devel python python-requests ccache systemd-units systemd-devel
          dnf install -y openssl-devel-engine || true
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          tags: true
      - name: Download Vue UI artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.vue.outputs.artifact_name }}
          path: .vuecache
      - name: Workaround safe directory
        run: git config --global --add safe.directory "$(pwd)"
      - name: Cache static dependencies
        uses: actions/cache@v4
        with:
          path: .staticlib
          key: staticlib-el-${{ matrix.releasever }}-${{ matrix.runner }}-${{ github.sha }}
          restore-keys: |
            staticlib-el-${{ matrix.releasever }}-${{ matrix.runner }}-
      - name: build
        run: |
          mkdir -p .staticlib
          export STATICLIB_CACHE="$PWD/.staticlib"
          export VUE_FILE_CACHE="$PWD/.vuecache"
          export TVH_EXTRA_CONFIGURE="--enable-vue_cache --disable-vue_build"
          ./configure --disable-dvbscan --disable-libfdkaac_static --disable-ffmpeg_static --disable-hdhomerun_static --disable-libfdkaac_static --disable-libopus_static --disable-libtheora_static --disable-libvorbis_static --disable-libvpx_static --disable-libx264_static --disable-libx265_static --enable-libfdkaac --enable-hdhomerun_client --enable-libsystemd_daemon --python=/usr/bin/python3 $TVH_EXTRA_CONFIGURE && make -C rpm build -j$(nproc)
      - name: copy-result
        run: cp rpm/RPMS/*/tvheadend*.rpm .
      - uses: actions/upload-artifact@v4
        with:
          name: Tvheadend-el-${{ matrix.releasever }}-${{ matrix.runner == 'ubuntu-latest' && 'x86_64' || 'aarch64' }}
          path: tvheadend*.rpm
          if-no-files-found: error
      - name: upload-cloudsmith
        run: support/cloudsmith.sh -n -f 'tvheadend*.rpm'
