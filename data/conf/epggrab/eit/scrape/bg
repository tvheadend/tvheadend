{
    "season_num": [
        "сезон ([0-9]{1,3})([^0-9]|$)",
        "[, ] сезон ([0-9]{1,3})([^0-9]|$)",
        "сез.? ([0-9]+)",
        "([0-9]+) сезон[, ]",
        "сериал[, ] с.([0-9]+)",
        "[, ] с. ([0-9]+)",
        "с. ([0-9]+), еп[.]",
        "еп. [0-9]+,.*, ([0-9]+), ?сез"
    ],
    "episode_num": [
        "([0-9]+) серия",
        "еп. ([0-9]+)",
        "[, ] ([0-9]+) еп[.]",
        "[, ] еп.([0-9]+)",
        "([0-9]+) еп.[,]",
        "сериал[, ] еп.([0-9]+)",
        "епизод ([0-9]+)",
        "Епизод ([0-9]+)",
        "[, ] ([0-9]+) епизод([^а]|$)",
        "([0-9]+) епизод([^а]|$)",
        "([0-9]+)[, ] последен",
        "[, ] ([0-9]+) и",
        "([0-9]+)-и епизод",
        "([0-9]+)-[вр]и епизод",
        "([0-9]+)-[тм]и епизод"
    ],
    "airdate": [
        ", ([0-9][0-9][0-9][0-9])"
    ],
    "genre" : [ {
        "Romance": ["(драма, романтичен)"],
        "Documentary": ["(документален)"]
    }
    ]
}
